<%- include('../partials/header') %>

    <h1>Danh sách nhà cung cấp</h1>
    <a href="/suppliers/new">➕ Thêm mới</a>
    <table border="1">
        <tr>
            <th>Name</th>
            <th>Address</th>
            <th>Phone</th>
            <th>Action</th>
        </tr>
        <% suppliers.forEach(s => { %>
            <tr>
                <td>
                    <%= s.name %>
                </td>
                <td>
                    <%= s.address %>
                </td>
                <td>
                    <%= s.phone %>
                </td>
                <td>
                    <a href="/suppliers/<%= s._id %>/edit">✏️</a>
                    <form action="/suppliers/<%= s._id %>/delete" method="POST" style="display:inline;">
                        <button type="submit">🗑️</button>
                    </form>
                </td>
            </tr>
            <% }) %>
    </table>

    <%- include('../partials/footer') %>